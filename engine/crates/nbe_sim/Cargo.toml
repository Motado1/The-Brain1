[package]
name = "nbe_sim"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# Simulation logic only (activation rules + action-potential propagation). Pure CPU; the
# GPU particle *rendering* of these pulses lives in the app and is verified on the workstation.
[dependencies]
nbe_data = { path = "../nbe_data" }
